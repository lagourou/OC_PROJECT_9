spring:
  datasource:
    url: jdbc:mysql://mysql:3306/medilabo?connectTimeout=10000&socketTimeout=10000&autoReconnect=true
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always
      encoding: UTF-8
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

jwt:
  secret: 94a2b5be43204b928b31c8a1aa29bc98afac9e4b3d1e4e0c8522e4ec21767914

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
